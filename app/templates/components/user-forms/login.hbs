{{#x-form as |f|}}
    {{f.email 'Initial value' 'Email'}}
    {{f.password 'Initial password' 'Password'}}
      {{#x-button as |b|}}
        {{b.success 'Login' onClick=(action "authenticate")}}
        {{b.warning 'Clear form' onClick=(action "clearForm")}}
      {{/x-button}}
{{/x-form}}